<h1>Upload a photo</h1>
	<div id="container">
		<%= form_for @photo do |f| %>

			<%= f.text_field :title, placeholder:"Title of your pic"%>
			<%= f.file_field :image%>

			<%= f.submit 'UPLOAD',class:"button" %>	

		<% end %>

	</div>