<div class="pic">
  <h4><%= photo.title %></h4> 
  <%= link_to image_tag(photo.image.url(:thumb)), photo.image.url, target: '_blank' %>

  <%= link_to 'Remove', photo_path(photo), method: :delete, data: {confirm: 'Are you sure?'}, class:'remove'%> 
</div>